/*!
Context module for WittCore

This module re-exports Context from note::Context for backward compatibility.
A Context represents a specific usage example of a lemma (word/phrase).
Each Note can have up to 5 Contexts.
*/

pub use crate::note::Context;
